---
description: interactive-experience-skills の古くなった記述をWeb検索で洗い出し、差分だけ報告する
argument-hint: [apply と書くとファイルへ反映まで行う。省略時は報告のみ]
---

モード: $ARGUMENTS

このコマンドは `interactive-experience-skills` のリファレンスのうち、**時間とともに古くなる記述**を調べ直します。

## 対象の見つけ方

リポジトリの正本（`_extracted/`）から、`<!-- volatile: YYYY-MM — ... -->` の印が付いたファイルを探します。

```bash
grep -rn "volatile:" _extracted/*/references/*.md
```

リポジトリの場所が分からない場合は `~/.claude/skills/` 配下を対象にし、その旨を最初に断ってください。ただし**書き換えるのは正本の `_extracted/` だけ**です。`~/.claude/skills/` を直接編集してはいけません（次の `install.sh` で上書きされて消えます）。

印のコメントには「このファイルの何が古くなるか」が書いてあります。**そこに書かれた項目だけを調べます。**ファイル全体を読み直して書き換えないでください。このコマンドの価値は、変わったものだけを見つけることにあります。

## 手順

### 1. 主張を抜き出す

印が付いたファイルごとに、印のコメントが名指ししている種類の記述を、検証可能な単位で列挙します。

良い抜き出し方の例:
- 「MediaPipe はオンデバイス姿勢推定の第一候補」→ 検証可能
- 「暗室・小空間（〜3m幅）は 5,000〜8,000 ANSI lm」→ 検証可能
- 「Obscura Digital は独立したスタジオではない」→ 検証可能

抜き出してはいけない例:
- 「追跡精度と学習効果は別物である」→ 設計上の立場であって事実の主張ではない。時間で古くならない
- 「確信が持てない時は黙る」→ 同上

**判断基準: 3年後に間違いになりうるか。**なりえないものは対象外です。

数値であっても、**人間の知覚特性や学習科学に基づくしきい値**（100ms を超えると自分の動きだと感じられない、など）は対象外です。技術が進んでも動きません。対象になる数値は、機材のスペック、価格、対応 fps のように**市場が動かすもの**だけです。

**印の項目に対応する記述がファイル内で特定できない場合は、推測で対象を広げないでください。**「この項目は対応箇所を特定できなかった」と報告に明記し、印の書き方を直すよう提案してください。

### 2. 調べる

抜き出した主張を、性質ごとにまとめて WebSearch にかけます。1件ずつ検索しないでください。

- ソフトウェア・ライブラリ（TouchDesigner / Unreal / Unity / Notch / Three.js / MediaPipe / Apple Vision / RTMPose 等）— 現行かどうか、後継が出ていないか、推奨される用途が変わっていないか
- 機材のクラスと価格帯 — 帯がずれていないか
- スタジオ・企業 — 存続しているか、買収・解散していないか
- 相場（客単価、月謝、サブスクの価格帯）— 桁が変わっていないか

**裏が取れなかったものは「変わった」と書かないでください。**「確認できなかった」と書きます。この2つを混ぜることが、このコマンドで最も高くつく失敗です。

### 3. 報告する

変化があったものだけを、次の形で出します。変化が無ければ「変化なし」と1行で終えてください。長い報告は価値ではありません。

```
ファイル:行
  現状の記述: <引用>
  分かったこと: <調べて分かった事実>
  出典: <URL>
  推奨: <書き換え案 / 注記を足す / 削除する / 様子見 / 選択肢が増えた>
```

**「選択肢が増えた」**は、既存の記述は間違っていないが、その分野に新しい有力な選択肢が出てきた場合に使います。この場合**書き換えは提案しないでください。**追加するかどうかは人間が決めます。何が増えたかを報告するだけに留めます。

そのうえで、印のコメント自体を更新すべきか（新しく古くなる項目が増えていないか）も一言添えてください。

### 4. 反映する（`apply` が指定された時だけ）

`$ARGUMENTS` に `apply` が含まれている時のみ、次を実行します。

1. 上の推奨のうち、**出典が示せたものだけ**を `_extracted/` に反映する
2. 変更したファイルの `<!-- volatile: YYYY-MM -->` の年月を今日の年月へ更新する
3. `./install.sh` を実行して配置と `.skill` の再パッケージを行う
4. 変更点を要約する（コミットはしない。ユーザーが確認してから行う）

`apply` が無い場合は、**ファイルを一切書き換えません。**報告だけです。

## やらないこと

- 印が付いていないファイル（`opportunity.md` / `sequences.md` / `learning-design.md` / `skill-modeling.md`）は対象外です。方法論と研究知見であり、Web検索で更新する対象ではありません
- 新しい技術やスタジオを見つけても、勝手に追加しないでください。既存の記述が正しいままかを確認するのがこのコマンドの仕事です。追加は人間が決めます
- SKILL.md 本体は書き換えません。判断基準は時間で古くなりません
