---
description: interactive-experience-skills に入っていない新しい有力手段を探し、候補キューへ追記する（リファレンスは書き換えない）
argument-hint: [領域名を指定するとその領域だけ。省略時は4領域すべて]
---

領域: $ARGUMENTS

このコマンドは `refresh-skills` の**第2トラック**です。役割が違うので混同しないでください。

| | `/refresh-skills` | `/scout-skills`（これ） |
|---|---|---|
| 問い | 書いてあることが今も正しいか | 書いていない有力な手段が出ていないか |
| 対象 | `<!-- volatile: -->` 印の項目 | 下の4領域 |
| 出力 | 差分報告（`apply` でファイルへ反映） | **`CANDIDATES.md` へ追記するだけ** |
| リファレンス | 書き換える（`apply` 時） | **一切書き換えない** |

`refresh-skills` には「新しい技術を見つけても勝手に追加しない」と明記されています。だから新しい手段は**構造上あちらからは絶対に入ってきません。**それを拾うのがこのコマンドの唯一の仕事です。

## 探す領域

`$ARGUMENTS` が空なら4領域すべて。指定があればその領域だけ。

1. **描画** — GPU API、シェーダー言語、レンダリングライブラリ、アルゴリズム描画、実空間の取り込み
2. **音響** — 合成方式、DSP、音楽理論ライブラリ、低遅延処理、ライブコーディング
3. **取得** — 姿勢・手・顔の推定、深度、センサー、オンデバイス推論
4. **配布** — 器（Web / ネイティブ / 空間）、バンドル重量、対応状況、権限まわり

## 手順

### 1. 現状を把握する

各領域について、スキルが**今どう書いているか**を先に読みます。読まずに探すと、既にあるものを「新しい」と報告します。

```bash
grep -l "" _extracted/*/references/*.md
```

主に見るのは `software.md` / `sound.md` / `feel.md` / `movement.md` / `solo-scale.md` / `motion-tech.md`。

### 2. 探す

領域ごとにまとめて WebSearch にかけます。1件ずつ検索しないでください。

探しているのは「新しくて話題のもの」ではなく、**既存の記述では出せない表現・判断を可能にするもの**です。速い・新しい・流行っている、は理由になりません。

### 3. ルーブリックにかける

見つけたものを、次の4項目で判定します。**4項目すべてを満たすものだけが候補になります。**

| # | 問い | 落ちる例 |
|---|---|---|
| 1 | 既存の手段では出せない**表現・判断**が出せるか | 既存ライブラリの高速版 |
| 2 | 個人・小規模で到達可能か。不能ならその旨を明記できるか | 大規模クラスタが前提 |
| 3 | **壊れ方が書けるか** | まだ誰も本番で使っていない |
| 4 | **どの既存記述に接続 or 置換されるか**が言えるか | 「どこかに足せそう」 |

**4で落ちるものが最も多く、それでいい。**リファレンスが太ると、モデルは読む量を絞り始め、厚くしたことで質が下がります。このコマンドの価値は追加数ではなく、**通らないものを通さないこと**です。

### 4. 候補キューへ追記する

`CANDIDATES.md` の末尾に、次の形で追記します。**リファレンスは触りません。**

```
## YYYY-MM / 領域 / 名前

出せるもの:   <既存では出せない表現・判断>
代償:         <重さ、対応状況、学習コスト、依存>
壊れ方:       <どう失敗するか>
接続先:       <ファイル名 §節。置換なら何を消すか>
出典:         <URL>
判定:         候補 / 見送り（理由）
```

すべて落ちた場合は「候補なし」と1行書いて終えます。**長い報告は価値ではありません。**

### 5. 最後に一言

印（`<!-- volatile: -->`）に、新しく古くなる項目が増えていないかを添えます。増えていれば、印の書き換えを提案します（実行はしない）。

## やらないこと

- **リファレンスを書き換えない。**昇格は人間が決めます
- 候補を3件以上出そうとしない。通らないなら0件でいい
- 裏が取れないものを候補にしない。「確認できなかった」と書きます
- SKILL.md 本体を触らない。判断基準は新技術では変わりません
- スタジオ・企業・製品の**精度や優劣を推測で語らない。**公開情報から言えるのは「何を返しているか」だけです

## 頻度

**四半期に1回 + イベント駆動**（主要ブラウザのリリース、Apple のプラットフォーム発表、大きなカンファレンス）。

月次にしないこと。この分野は1ヶ月で意味のある変化が起きず、「候補なし」を読み飛ばす習慣がつくと、本当に変わった時に見落とします。
