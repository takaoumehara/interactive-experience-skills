# メンテナンス・ルーティン

このスキル群は、技術の現況に依存する記述を持っている。**放置すると、正しかった記述が静かに間違いに変わる。**それを防ぐための、回す手順。

スケジューラには載せていない。**理由は、機械の予約より、引き金と手順が決まっていることの方が長持ちするから。**cron はマシンが変われば消え、通知は数回で読み飛ばされるようになる。ここに書いてあるのは、思い出した時に30分で完了できる形の手順である。

---

## 1. いつ回すか

### 定期 — 四半期に1回

2月 / 5月 / 8月 / 11月。**月次にしない。**この分野は1ヶ月で意味のある変化が起きず、「変化なし」の報告を読み飛ばす習慣がつくと、本当に変わった時に見落とす。

### 引き金 — こう感じたら、四半期を待たずに回す

**これが定期より重要。**日付ではなく、次の出来事が本当の合図。

| 引き金 | 回すもの |
|---|---|
| Apple のプラットフォーム発表があった | `/scout-skills 描画` `/scout-skills 配布` |
| 主要ブラウザで GPU / メディア系の対応が動いた | `/scout-skills 描画` `/scout-skills 音響` |
| 姿勢推定・オンデバイス推論で新しいものを見た | `/scout-skills 取得` |
| **スキルの回答に古い前提が混じっているのに気づいた** | `/refresh-skills`（該当領域） |
| **案件で「これ、リファレンスに無いな」と思った** | その場で `CANDIDATES.md` に手で書く |
| 大きなカンファレンスの後 | `/scout-skills`（全領域） |

**最後の2つが最も価値が高い。**実際に使っていて感じた欠落は、検索では絶対に見つからない。

---

## 2. 手順 — 30分

順番に意味がある。**検証を先にやる。**新しいものを探す前に、今書いてあることが正しいかを確定させないと、古い記述の上に新しい記述を積むことになる。

### ① 検証（10分）

```
/refresh-skills
```

`<!-- volatile: -->` 印の記述をWebで調べ直し、**変わったものだけ**を出す。ファイルは触らない。

内容を読んで納得したら:

```
/refresh-skills apply
```

出典を示せたものだけが反映され、印の年月が更新され、`install.sh` まで走る。

### ② 探索（15分）

```
/scout-skills
```

書いていない有力な手段を4領域（描画・音響・取得・配布）で探し、`CANDIDATES.md` へ追記する。**リファレンスは一切書き換えない。**

領域を絞るなら `/scout-skills 音響` のように指定する。引き金で回す時は、関係する領域だけでいい。

### ③ 判定（5分）

`CANDIDATES.md` の新しい候補を読み、採用するものを決める。ルーブリック4項目を全部満たしているかを見る。

1. 既存の手段では出せない**表現・判断**が出せるか
2. 個人・小規模で到達可能か
3. **壊れ方が書けるか**
4. **どの既存記述に接続 or 置換されるか**が言えるか

**採用するなら、必ず置換か統合を同時に決める。**言えないなら採用しない。ここが唯一の防波堤で、外すとリファレンスが太り、スキルは読む量を絞り始め、厚くしたことで質が下がる。

### ④ 反映と記録

採用したものを `_extracted/` へ書き、次を実行する。

```bash
./install.sh    # 参照切れと name 不一致を検証し、配置して .skill を再パッケージ
./package.sh    # 配布用 ZIP（配布する時だけ）
```

最後に、下の実行ログへ1行足してコミットする。

---

## 3. チェックリスト

回し終わった時に、これが全部言えること。

- [ ] `/refresh-skills` を走らせ、変わったものを確認した
- [ ] 反映したものには出典がある（「たぶん変わった」を入れていない）
- [ ] `/scout-skills` を走らせ、候補を `CANDIDATES.md` に記録した
- [ ] 採用した候補には、接続先または置換対象が書いてある
- [ ] **行数が増えたなら、何を削ったか言える**
- [ ] `./install.sh` が事前検証を通過した
- [ ] 実行ログに1行追加した

---

## 4. やらないこと

- **リファレンスを「充実させる」ために回さない。**このルーティンの成果は追加数ではない。0件で終わる回が普通であり、それが健全
- **volatile 印の無いファイルを調べ直さない。**`protocol.md`、`learning-design.md`、`skill-modeling.md`、`opportunity.md`、`sequences.md` は方法論であり、技術の変化で古くならない
- **SKILL.md 本体の判断基準を書き換えない。**「制約はデザイン材料である」「確信が持てない時は黙る」は新しいライブラリが出ても変わらない
- **裏が取れないものを「変わった」と書かない。**「確認できなかった」と書く。この2つを混ぜるのが最も高くつく失敗
- **プロジェクト固有の企画・調査をスキルへ入れない。**`notes/` へ置く。スキルに入れると、全ユーザーに同じ答えを再生産させることになる

---

## 5. 行数の予算

| 時点 | interactive-experience-collective |
|---|---|
| 2026-07 | 1,170 行 |
| 2026-08 | 1,542 行 |
| **上限の目安** | **2,000 行** |

**2,000行を超えたら、追加をやめて統合の回にする。**リファレンスが太るとモデルは読む量を絞り始め、厚くしたことで質が下がる。増やす回と削る回を交互にする方が、結果として密度が上がる。

```bash
wc -l _extracted/*/references/*.md _extracted/*/SKILL.md | tail -1
```

---

## 6. 実行ログ

回すたびに1行足す。**「変化なし」も記録する。**記録が無いと、前回いつ回したか分からなくなり、それがこのルーティンの死因になる。

| 日付 | 引き金 | 検証 | 探索 | 採用 | 行数 |
|---|---|---|---|---|---|
| 2026-08-02 | 手動（技術リストの持ち込み） | — | 手動 | feel.md 新設、描画・音響・触感の語彙一式、計測の現実 | 1,542 |

<!-- 次の行をここに足す -->
