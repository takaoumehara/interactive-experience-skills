# CHANGELOG

このリポジトリの3スキルの更新履歴。運用は `maintenance/UPDATE-ROUTINE.md`。

書式: 変更 / 見送り / 次回の監視対象。**見送りも記録する** — 「調べた上で反映しなかった」ことが分かると、次回の判断が速くなる。

---

## 2026-07-28

初回の事実更新。あわせて、継続更新のための構造とルーチンを導入した。

### 構造の変更 — 既存の鮮度対策の上に「記録」を足した

既に `<!-- volatile: -->` マーカーと `/refresh-skills` があり、**「どこが腐るか」と「変わったかを調べる」は解けていた。**足りなかったのは「**で、今はどうなのか**」を日付と出典つきで残す場所と、それを定期的に回す仕組みである。

- **時点依存の事実を `references/current.md` に隔離した**（3スキルすべて）。型番・価格・バージョン・入手性・法規制はここに集約し、原理・構造の記述からは切り離した。このファイル自身にも volatile マーカーを付けてある
- **`volatile` マーカーの扱いを3段階にした。**マーカーが付いた記述を使う時は、①まず `current.md` を見る（検索不要）②載っていないか `snapshot_date` が6ヶ月以上前なら検索 ③確認せずに書くなら時点を明示。**マーカー＝索引、`current.md`＝答えの置き場**という役割分担
- `maintenance/UPDATE-ROUTINE.md` を追加。月次ジョブにそのまま渡せる指示文、採否基準、分量の規律、実行環境（Claude Code / GitHub Actions / クラウドセッション）別の push の扱い。**検出は `/refresh-skills` を内部で呼んで済ませ、同じ検索ロジックを二重に書かない**
- `maintenance/sources.md` を追加。監視する情報源を9領域ぶん。**マーカーが増えたら監視領域も増やす**運用にした
- `install.sh` に鮮度チェックを追加。`current.md` が180日以上前、または存在しない場合に警告を出す（配置は止めない）
- SKILL.md の metadata に `last_reviewed` を追加。version を 1.1 → 1.2 に

### 事実の更新（すべて2026年7月時点、出典は各 current.md）

**姿勢推定・身体トラッキング**
- MediaPipe は Web/Tasks 側のみが活発で、C++フレームワーク側は更新が止まっている。**Web の GPU デリゲートは WebGL2 で、WebGPU は使っていない**（バンドル実物で検証）
- TensorFlow.js とその pose-detection は事実上凍結。MoveNet はその上に乗っている
- mmpose（RTMPose）は2024年7月から更新停止。「凍結された優良資産」として位置づけを変更
- **ARKit Body Tracking が iOS 26 系で機能不全**（報告済み・未解決）。`solo-scale.md` と `movement.md` の ARKit 記述に検証を促す注記を追加。「iPhone Proを置けば3D全身が取れる」という前提を企画のクリティカルパスに置かない旨を明記
- カメラ2台の三角測量パイプラインが実用段階。**「3D関節位置が欲しい＝深度カメラ」という選定を反転**させ、`hardware.md` のセンサー表の第一候補を RGB＋姿勢推定に変更

**機材・費用**
- Azure Kinect DK の EOL と後継（K4Aラッパー経由でコード移植可）を反映
- Ultraleap の売却・分割により、空中ハプティクスを調達不能として扱う旨を追加
- **COB LED の価格プレミアム消滅**を反映し、「来場者が触れる/低い設置高/長期常設なら COB を初期設計に入れる」を `hardware.md` に追加
- プロジェクターの**ルーメン単価の非線形性**（中位帯が最も有利）を `hardware.md` に追加
- **メモリ逼迫によるGPU高騰と新世代の不在**を反映。「毎年GPUを更新して性能を買う」前提を削除し、「今ある世代で3年戦う」「計算機は最初に押さえる固定費かつ調達リスク」に置き換え（`hardware.md` / `software.md` / `solo-scale.md`）
- XRは Quest 3 系で3年戦う前提へ

**ソフトウェア**
- Unreal 5.8 が最後の予定 UE5 リリースであること、UE6 が2027年末EA であることを current.md に記録。**常設案件では「そのバージョンが何年運用できるか」が選定理由の一部になる**旨を `software.md` に追加
- Unity の Runtime Fee 撤回後の現況、TouchDesigner / Notch の現行版と価格体系を記録
- WebGPU が全主要ブラウザで出荷済み、ただし three.js の WebGPURenderer は experimental のまま
- **3D Gaussian Splatting を `software.md` のアーキテクチャパターンに追加**（パターンE）。エンジンごとのネイティブ対応差が選定に直結する

**リアルタイムAI**
- リアルタイム映像生成が実用段階に入ったことを反映。`software.md` のパターンDを「できるか」から「**どこで回すか（ローカルGPUかクラウド従量か）**」の判断に書き換え
- 映像の理解（VLM）はリアルタイムに届いていないため、軽量CV＋低頻度のモデル呼び出しというハイブリッドを明記

**法規制（このスキル群に初めて明示的に導入した領域）**
- 全21ファイルを通じて EU AI Act・個人情報保護法への言及がゼロだった。3スキルの current.md に法規制の節を新設
- **設計上の分水嶺を「顔を撮るか」ではなく「一意識別するか/生体テンプレートを保存するか」として明示。**これは事後に足せない設計判断であるため、診断・企画段階で扱う
- EU AI Act: 高リスク義務の2027年12月への延期と、**2026年8月2日から適用される透明性義務**
- 日本: 令和8年改正個人情報保護法の公布（2026年7月17日）、特定生体個人情報、16歳未満の法定代理人同意、AI学習利用の条件付き適法化
- 米国: COPPA改正規則の全面適用、BIPA のエクスポージャー低下

### 未調査（正直に記録する）

- **`sound.md` が名指ししている時点依存項目**（Web Audio / Ableton / Max / SuperCollider の対応状況、**権利処理の窓口と料率**、IR素材の入手先）は今回調査していない。collective の current.md に「未調査」と明記し、次回の宿題にした。`sound.md` の volatile マーカーの年月は**動かしていない**（動かすと「見た」という嘘になる）
- `product-business.md` の道場・スタジオの経済（月謝相場、サブスク価格帯）も同様に未調査

### 見送り

- **YOLOv12 / YOLOv13 の pose 対応** — 一次情報を確認できなかった。現行ラインは YOLO11 → YOLO26 と確認できたため、そちらのみ記載
- **Notch と Disguise の資本関係** — 買収の有無を一次情報で確認できず。記載しない
- **視線推定・感情推定・群衆解析・音響イベント検知の2026年精度** — 一次情報に到達できず。技術面は記載せず、規制面の接続のみ記載。**次回の宿題**
- **Cables.gl の現況** — 2025年10月以降の情報が乏しく、記載を控えた
- 各ツールのマイナーバージョンアップ、未出荷の発表製品（RealSense D585 Pro、Meta Quest 4、UE6 等）は本文の推奨を変えず、current.md の「監視中」に記載

### 次回の監視対象

- **日本の政令で歩容（gait）が特定生体個人識別符号に含まれるか**（最重要。含まれれば「動きだけだから安全」という前提が崩れる）
- 視線・感情・群衆解析の実装コストと精度（今回未達）
- MediaPipe Web の WebGPU 対応、three.js WebGPURenderer の experimental 解除
- ARKit Body Tracking の修正
- Unreal 6 Early Access、Meta Quest 4、RealSense D585 Pro の出荷
- メモリ市況の緩和
- 運動学習研究側の外的焦点・文脈干渉効果の大規模追試（**原理層に触れる唯一の監視項目**）
