---
description: interactive-experience-skills のメンテナンスを検証→探索→判定の順に1回分まとめて回す
argument-hint: [領域を指定すると探索をその領域に絞る。verify と書くと検証だけ]
---

範囲: $ARGUMENTS

`ROUTINE.md` に書かれた1回分のメンテナンスを、順番通りに実行します。**順番に意味があります。**新しいものを探す前に、今書いてあることが正しいかを確定させないと、古い記述の上に新しい記述を積むことになります。

まず `ROUTINE.md` を読み、前回の実行ログを確認してから始めてください。前回から何ヶ月経っているかで、確認の必要性が変わります。

## 手順

### ① 検証

`refresh-skills` の手順を実行します（`~/.claude/commands/refresh-skills.md`、リポジトリなら `refresh-skills.md`）。

`<!-- volatile: -->` 印の記述をWebで調べ直し、**変わったものだけ**を報告します。この段階ではファイルを書き換えません。

`$ARGUMENTS` に `verify` が含まれていれば、**ここで止めて報告し、終了します。**

### ② 探索

`scout-skills` の手順を実行します。

`$ARGUMENTS` に領域名（描画 / 音響 / 取得 / 配布）が含まれていれば、その領域だけを探索します。含まれていなければ4領域すべて。

結果は `CANDIDATES.md` へ追記するだけで、**リファレンスは書き換えません。**

### ③ 判定して提示する

①と②の結果を、次の1つの表にまとめます。**2つの別々の報告として出さないでください。**判断に必要なのは統合された1枚です。

```
| 種別 | 対象 | 分かったこと | 推奨 |
|------|------|-------------|------|
| 検証 | ファイル:行 | <調べて分かった事実> | 書き換え / 注記 / 削除 / 様子見 |
| 候補 | 領域 / 名前 | <既存では出せないもの> | 採用 / 見送り（理由） |
```

各候補には、ルーブリック4項目のうち**どれで落ちたか**を明記します。特に④「どの既存記述に接続 or 置換されるか」で落ちたものは、**そう書いてください。**ここが最も多く、それが正常です。

### ④ 反映は聞いてから

**勝手に反映しません。**上の表を出したうえで、次を尋ねます。

```
反映しますか。番号で指定してください（複数可）。何も反映しない場合は「なし」。
```

指定されたものだけを `_extracted/` へ反映し、`<!-- volatile: -->` の年月を今日へ更新し、`./install.sh` を実行します。

**採用するものが行数を増やすなら、何を削るか・どこへ統合するかを同時に決めます。**言えないものは反映しません。この規律を外すとリファレンスが太り、スキルは読む量を絞り始め、厚くしたことで質が下がります。

### ⑤ 記録する

反映の有無にかかわらず、`ROUTINE.md` の実行ログへ1行足します。**「変化なし」も記録します。**記録が無いと前回いつ回したか分からなくなり、それがこのルーティンの死因になります。

```
| YYYY-MM-DD | <引き金> | <検証の結果> | <候補数> | <採用したもの/なし> | <行数> |
```

行数は次で取ります。

```bash
wc -l _extracted/*/references/*.md _extracted/*/SKILL.md | tail -1
```

2,000行を超えていたら、**その旨を報告に明記し、次回を統合の回にすることを提案してください。**

## 報告の長さ

**変化も候補も無ければ、3行で終えてください。**

```
検証: 変化なし（volatile 印 5ファイル）
探索: 候補なし
ログに記録しました。次は <3ヶ月後の月> 頃、または引き金が引かれた時。
```

長い報告は価値ではありません。このコマンドの価値は、**変わったものだけを見つけること**と、**通らないものを通さないこと**にあります。
